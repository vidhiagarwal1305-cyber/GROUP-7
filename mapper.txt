import sys
import csv

with open("student_performance.csv", "r") as f:
    reader = csv.DictReader(f)
    for row in reader:
        gender = row["Gender"].strip()
        if gender:
            print(gender + "\t" + "1")